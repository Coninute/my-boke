import { SyncHook } from './SyncHook';
export declare class SyncWaterfallHook<
  T extends Record<any, unknown>,
  C = null,
> extends SyncHook<[T], C, T> {
  constructor(context?: C);
  emit(data: T): T;
}
