/**
 * Give the current task one frame of time (default is 17ms).
 * If it exceeds one frame, the remaining tasks will be put into the next frame.
 */
export declare const loopSlice: (
  l: number,
  fn: (i: number) => void | boolean,
  taskTime?: number,
) => Promise<void>;
